# Paper Table And Figure Captions - English

## Tables

Table 1. Main result summary for the selected Track A canonical-like VN30 hourly model. The table reports final pooled directional accuracy, baseline references, lift values, row count, coverage, and the claim boundary for the L2 Logistic h40 candidate.

Table 2. Stability summary for ticker, time, regime, and validation-final diagnostics. The table reports slice-level averages and classifications used to bound the result as exploratory improved baseline60 evidence.

Table 3. Regime slice summary for the selected model. Regime rows show directional accuracy, majority baseline accuracy, lift, row count, and whether each regime reaches the 60% and 62% thresholds.

Table 4. Full ticker-level accuracy table for the selected final evaluation window. The table reports accuracy, rows, correct and incorrect predictions, majority baseline accuracy, and lift for each VN30 ticker.

Table 5. Full monthly accuracy table for the selected final evaluation window. The table reports month-level accuracy, row count, majority baseline accuracy, and lift.

Table 6. Full quarterly accuracy table for the selected final evaluation window. The table reports quarter-level accuracy, row count, majority baseline accuracy, and lift.

Table 7. Data scope summary for the hourly stock, daily stock, and index context artifacts. The table documents available time windows and limitations from repository readiness and forensics artifacts.

Table 8. Literature comparison placeholder table. External literature values require source verification before numeric comparison claims can be made.

## Figures

Figure 1. Research design flow for the Track A VN30 hourly evidence package. The flow separates data readiness, feature engineering, validation selection, final scoring, stability audit, and claim boundary steps.

Figure 2. Final directional accuracy comparison between the selected L2 Logistic h40 model and baseline references. The selected model reached 61.51% final pooled directional accuracy, exceeding the majority baseline and two model-reference baselines, but not reaching the 62% target threshold.

Figure 3. Lift over baseline references for the selected L2 Logistic h40 model. The figure reports lift over the majority baseline, Logistic h40 baseline, and historical RF h60 reference.

Figure 4. Ticker-level final directional accuracy for the selected model. The figure shows cross-sectional dispersion and supports a moderate ticker-stability interpretation rather than a uniform target62 claim.

Figure 5. Monthly final directional accuracy for the selected model. The month-level path shows substantial time variation, so the result is not presented as time-stable target62 evidence.

Figure 6. Quarterly final directional accuracy for the selected model. The quarterly chart summarizes time concentration and the mixed stability boundary.

Figure 7. Regime slice accuracy for the selected model. Only one of the three regime slices reaches 60% and 62%, so regime stability is not established.

Figure 8. Bootstrap confidence interval for final pooled directional accuracy. The interval is derived from ticker-weighted resampling and is reported as uncertainty evidence, not as a target62 success claim.

Figure 9. Validation-final accuracy gap for the selected model. The final score is 9.63 percentage points above validation accuracy, so the paper discloses a high positive validation-final gap.

Figure 10. Data scope and coverage timeline for stock and index artifacts. The timeline documents that hourly stock evidence starts in 2023 while daily stock and daily index context extend further back.
