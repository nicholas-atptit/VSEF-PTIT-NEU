# Prompt Run: 15_experiment_tracking_validation

## Commit / Diff Basis
- Uncommitted changes (Best-effort reconstruction)
- Timestamp: 2026-04-02 03:23 AM (Local Time)

## Objective
- Professionalize the ML training and data ingestion pipelines by implementing a lightweight experiment tracking system and a robust data quality validation layer.

## Files Added
- `src/ml/experiment_tracker.py`
- `src/validators/data_quality.py`
- `tests/test_data_quality.py`

## Files Modified
- `scripts/train_ml_tickers.py` (Integrated `ExperimentTracker` and `DataQualityValidator`)
- `src/historical/backdate.py` (Integrated `DataQualityValidator`)

## Detailed Change Summary

### 1. `src/ml/experiment_tracker.py`
- Implemented `ExperimentTracker` class to log training runs to `reports/experiments.jsonl`.
- Logs include `run_id`, `ticker`, `label_type`, `model_type`, `feature_count`, `metrics`, and timing metadata.
- Uses JSONL format for easy appending and parsing.

### 2. `src/validators/data_quality.py`
- Implemented `DataQualityValidator` with two primary methods:
  - `validate_ohlcv`: Checks for missing columns, negative values, price inversions (High < Low), and suspicious volume.
  - `validate_features`: Checks for excessive null rates (threshold: 20%) and infinite values in computed feature matrices.

### 3. `scripts/train_ml_tickers.py`
- Modified to instantiate `ExperimentTracker` at the start of a batch session.
- Added `DataQualityValidator` calls before training each ticker to ensure OHLCV and feature matrix integrity.
- Integrated `tracker.log_experiment` at the end of each ticker's training loop.

### 4. `src/historical/backdate.py`
- Integrated `DataQualityValidator` into the `BackdateIngestor.run` method.
- Validates data fetched from API (vnstock/DNSE) before it's saved to the database.

## Algorithms / Models / Methods Added or Changed
- **Validation Rules**: Standardized logic for identifying anomalous market data (e.g., negative prices, $High < Low$).
- **Statistical Pruning**: Added null-threshold checking (20%) for features to prevent training on sparse data.

## Interface Changes
- New Imports: `from src.ml.experiment_tracker import ExperimentTracker`.
- New Imports: `from src.validators.data_quality import DataQualityValidator`.
- Logic: `scripts/train_ml_tickers.py` now accepts `ExperimentTracker` in its internal `train_ticker` signature via `kwargs`.

## Persistence Changes
- **File System**: New log file created at `reports/experiments.jsonl`.
- **Database**: No schema changes, but higher data integrity guaranteed for `raw_prices`.

## Tests
- Added `tests/test_data_quality.py`.
- Commands: `pytest tests/test_data_quality.py -v`

## Risks / Follow-ups
- **Log Management**: `reports/experiments.jsonl` will grow over time; consider log rotation or migration to a DB-backed tracker.
- **Strictness**: Current validation `raise_on_error=True` might stop batch runs for single-ticker anomalies. Consider a "quarantine" instead of hard failure.

---
*Generated by Antigravity AI - 2026-04-01*
