experiment:
  id: EXP-ST-001
  name: Diebold-Mariano Forecast Error Test
  description: Compare model forecast errors against baseline errors.
  phase: 6
  owner: Project team / maintainer
  created_at: "2026-05-09"

source_artifacts:
  forecasting_experiments:
    - EXP-FC-001
    - EXP-FC-003
  feature_experiments:
    - EXP-FA-000
  experiment_output_root: outputs/experiments
  forecasting_report_dir: reports/forecasting_core
  feature_report_dir: reports/feature_analysis

comparisons:
  baselines:
    - persistence
    - zero_return
  models:
    - sarimax
    - ets
    - xgboost
    - lightgbm
  losses:
    - squared
    - absolute
  horizons:
    - 1
    - 3
    - 5

outputs:
  report_dir: reports/robustness
  output_csv: reports/robustness/dm_test_results.csv
